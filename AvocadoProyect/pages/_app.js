import Layout from "../components/Layout"

function MyApp({ Component, pageProps }) {
    return (
            <Component {...pageProps} />
        // <Layout>
        // </Layout>
    )
}
  
export default MyApp